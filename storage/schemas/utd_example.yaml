meta:
  type: UTD

schema:
  - type: row
    trigger:
      text: 'Счет-фактура №'
      position: same
    properties:
      - name: string_property1
        type: string
      - name: number_property1
        type: integer
  - type: row
    trigger:
      text: 'Грузополучатель и его адрес:'
      position: same
    properties:
      - name: string_property2
        type: string
  - type: collection
    name: items
    trigger:
      text: А
      position: before
    properties:
      - name: code
        type: string
        is_control: true
      - name: name
        type: string
